/** Injected at build from package.json (see vite.config.ts). */
export const APP_VERSION = import.meta.env.VITE_APP_VERSION ?? '0.0.0';
export const APP_NAME = import.meta.env.VITE_APP_NAME ?? 'elite-mob.github.io';
